A frequency table counts the number of occurrences in each class of data;

Used to create histograms and frequency polygons.

For quantitative variables, a *groupd frequency table* is generally used.
